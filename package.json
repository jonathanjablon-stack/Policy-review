{
  "name": "stop-loss-policy-review-workbench",
  "version": "16.1.0",
  "private": true,
  "description": "Local-first deterministic stop-loss contract review and comparison workbench",
  "scripts": {
    "build": "node scripts/build.js",
    "calibrate:corpus": "node scripts/calibrate-corpus.js",
    "test": "node --test tests/*.test.js",
    "verify": "npm test && npm run build && node scripts/verify-build.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
